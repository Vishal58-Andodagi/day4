version: "3"

services:

  mongo:
    image: mongo

  backend:
    build: ./backend

  frontend:
    build: ./frontend

  nginx:
    image: nginx

    ports:
      - "80:80"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf

